# Cyrus Docker Environment Configuration
# Copy this file to .env.docker and fill in your credentials
#
# IMPORTANT: Never commit .env.docker to version control!

# ==============================================================================
# REQUIRED: Claude Authentication (choose one)
# ==============================================================================

# Option 1: Direct API key (recommended for development)
ANTHROPIC_API_KEY=

# Option 2: OAuth token (for production/team use)
# CLAUDE_CODE_OAUTH_TOKEN=

# ==============================================================================
# REQUIRED: Linear OAuth (for self-hosted mode)
# ==============================================================================

# Create an OAuth app at: https://linear.app/settings/api/applications
# Required scopes: read, write, issues:create, comments:create
LINEAR_CLIENT_ID=
LINEAR_CLIENT_SECRET=

# Webhook secret (recommended for security)
# Generate with: openssl rand -hex 32
LINEAR_WEBHOOK_SECRET=

# Enable direct webhooks mode (required for Docker setup)
LINEAR_DIRECT_WEBHOOKS=true

# ==============================================================================
# REQUIRED: External URL (ngrok tunnel)
# ==============================================================================

# The public URL where Linear can reach your Cyrus instance
# Get this from ngrok after starting the tunnel: ngrok http 3456
# Example: https://abc123.ngrok-free.app
CYRUS_BASE_URL=

# ==============================================================================
# OPTIONAL: ngrok Configuration
# ==============================================================================

# Your ngrok authtoken (for programmatic tunnel creation)
# Get from: https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTHTOKEN=

# ==============================================================================
# OPTIONAL: Git Configuration
# ==============================================================================

# Git identity (used for commits)
GIT_USER_NAME=
GIT_USER_EMAIL=

# GitHub personal access token (for gh CLI and private repos)
# Create at: https://github.com/settings/tokens
# Required scopes: repo, read:org
GITHUB_TOKEN=

# ==============================================================================
# OPTIONAL: Server Configuration
# ==============================================================================

# Cyrus server port (default: 3456)
CYRUS_SERVER_PORT=3456

# Cyrus home directory on host (default: ~/.cyrus)
# This is mounted into the container at /root/.cyrus
# CYRUS_HOME=~/.cyrus
